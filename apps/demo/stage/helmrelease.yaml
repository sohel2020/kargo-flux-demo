apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: demo
  namespace: demo-stage
spec:
  interval: 1m
  chart:
    spec:
      chart: demo
      version: 2.0.1
      sourceRef:
        kind: HelmRepository
        name: demo
        namespace: flux-system
  values:
    replicaCount: 2
